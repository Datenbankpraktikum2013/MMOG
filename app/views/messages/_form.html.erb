<%= form_tag(messages_path)%>
    <p><strong><%= label_tag ('Empfänger') %></strong></p>
      <%= text_field_tag('recipient') %>
    </p>
    <p><strong><%= label_tag ('Betreff') %></strong></p>
      <%= text_field_tag('subject') %>
    </p>
    <p><strong><%= label_tag ('Text') %></strong></p>
      <%= text_area_tag('body') %>
    </p>
    <p><%= submit_tag 'Annehmen' %></p>
</form>
<%= link_to 'Zurück', messages_path %>
