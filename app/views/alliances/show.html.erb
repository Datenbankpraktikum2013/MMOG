<p id="notice"><%= notice %></p>

<p>
  <h1><%= @alliance.name %></h1>
</p>
<p>
	<strong>Gründer:</strong>
	<%= @alliance.user.username %>
</p>

<p>
	<%= @alliance.description %>
</p>

<p>
	<strong>Mitglieder:</strong><br />
	<table border="1px">
		<tr>
			<th>Name</th>
			<th>Spacecash</th>
			<th>Userwertung</th>
		</tr>
		<% @alliance.users.each do |user| %>
			<tr>
				<td><%=user.username%></td>
				<td><%=user.money%></td>
				<td><%=user.score%></td>
			</tr>
		<% end %>
	</table>
</p>
	
<% if current_user==@alliance.user%>
	<%= link_to 'Bearbeiten', edit_alliance_path(@alliance) %> |
<%end%>
<%= link_to 'Zurück', alliances_path %>
