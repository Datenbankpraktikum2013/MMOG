<p>
	<div class="btn-group tip" title="Hast du keine Lust mehr, die Rolle des Gründers zu spielen? Gebe die schwere Bürde an einen anderen Nutzer ab. Danach kannst du auch die Allianz verlassen. Natürlich nur, wenn du willst.">
	    <a class="btn btn-large btn-warning dropdown-toggle" data-toggle="dropdown" href="#" %>
	    	<font color=white>Zurücktreten</font>
	    	<span class="caret"></span>
	    </a>
	    <ul class="dropdown-menu">
			<% @alliance.users.each do |user| %>
			<li><% if current_user!=user %>
					<%= link_to user.username, { :action => "change_founder", 'uid' => user.id}, :method => :post, class: 'link' %>
				<% end %>
			</li>
			<% end %>
	    </ul>
	</div>
</p>
<%= button_to 'Allianz auflösen', {:controller => "alliances", :action => "destroy"} , :method=>:delete, data: { confirm: 'Bist du dir sicher, dass du die Allianz auflösen willst?' }, class: 'btn btn-large btn-danger' %>
<script>
  $(function(){
    $('.tip').tooltip();
  });
</script>