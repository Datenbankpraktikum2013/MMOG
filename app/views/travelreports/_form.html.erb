<%= form_for(@travelreport) do |f| %>
  <% if @travelreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@travelreport.errors.count, "error") %> prohibited this travelreport from being saved:</h2>

      <ul>
      <% @travelreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mode %><br>
    <%= f.number_field :mode %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
