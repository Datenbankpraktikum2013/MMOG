<p id="notice"><%= notice %></p>

<div class="planetordner">

  <div class="sunsysinf">

    <strong>Name des Sonnensystems: <%= @sunsystem.name %></strong>
    <strong><%= link_to '↶', galaxy_path(@sunsystem.galaxy) %></strong>

  </div>

  <% @sunsystem.planets.each do |p| %>

      <a class="planetspos planetspos<%= p.z.to_s %>" href="<%= planet_path(p)%>" > <div>

        <% if !p.z.nil? then %>
            <%= image_tag('/images/icons/eisen32px.png')%>

        <% end %>

      </div></a>

  <% end %>


</div>

