<p id="notice"><%= notice %></p>
<%= form_tag("build", method: "post") do %>
<h3><%= "Available Ships on planet #{@planet.name}" %></h3>
<table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="example">


<% unless @shipshelper.get_available_ships(@planet).empty? %>
  
    <tr>
	    <td>Name</td>
	    <td>Construction time</td>
	    <td>Ores</td>
	    <td>Crystals</td>
	    <td>Credits</td>
	    
	    <td></td>
    </tr>
    <tr>
    <% @shipshelper.get_available_ships(@planet).each do |s| %>
	    <td><%= s.name %></td>
	    <td><%= s.construction_time %></td>
	    <td><%= s.ore_cost %></td>
	    <td><%= s.crystal_cost %></td>
	    <td><%= s.credit_cost %></td>
	    
	    <td><%= text_field(:ship, s.id ,:size => "4") %></td>
    </tr>
    <% end %>


</table>
<%= hidden_field_tag 'planet', @planet.id.to_s %>
<%= submit_tag("Build") %> 
<%= link_to 'Zurück', controller: "starport" %>
    <% end %>
  
  <% end %>